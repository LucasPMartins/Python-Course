#include<stdio.h>
#include<stdlib.h>

int main (){
    int x,y,z,R;
    scanf ("%d", &x);
    scanf ("%d", &y);
    scanf ("%d", &z);

    R = x + y + z ;
    
    printf("O resultado da soma (R) eh %d", R);
    return 0;
}